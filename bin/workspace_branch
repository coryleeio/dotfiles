#!/bin/bash
mkdir -p $HOME/Workspace
cd $HOME/Workspace
for D in *; do
    if [ -d "${D}" ]; then
 	echo ""
        echo "===== ${D} ====="   # your processing here
        pushd ${D} > /dev/null
 	git branch
 	popd> /dev/null
    fi
done
